# Security Policy

Report vulnerabilities to Jonas directly - do not open public issues
with exploit details. Jobs are read-only by default; any job requesting
write scope needs explicit owner approval. No tokens, keys, or webhook
secrets in this repo, job prompts, or logs (hashes/existence only).
